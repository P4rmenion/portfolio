import { Inter, Josefin_Sans } from 'next/font/google'

export const inter = Inter({ subsets: ['latin'], display: 'swap' })
export const josefin_sans = Josefin_Sans({ subsets: ['latin'], display: 'swap' })
